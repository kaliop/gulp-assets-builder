{
  "name": "assets-builder",
  "private": true,
  "version": "4.0.0",
  "engines": {
    "node": ">=4.0, <7"
  },
  "scripts": {
    "build": "cross-env NODE_NOTIFIER=1 gulp build",
    "watch": "cross-env NODE_NOTIFIER=1 gulp watch"
  },
  "devDependencies": {
    "cross-env": "^4.0",
    "gulp": "^3.9",
    "gulp-task-maker": "^1.0"
  },
  "dependencies": {
    "jquery": "^3.2",
    "normalize.css": "^6.0"
  },
  "browserslist": [
    "last 3 versions",
    "ios >= 9",
    "android >= 5",
    "ie >= 11"
  ]
}
