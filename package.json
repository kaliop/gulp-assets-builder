{
  "name": "assets-builder",
  "private": true,
  "version": "5.0.0",
  "engines": {
    "node": ">=6.5"
  },
  "scripts": {
    "build": "gulp build",
    "watch": "gulp build watch --series"
  },
  "devDependencies": {
    "assets-builder-minjs": "./tasks/minjs",
    "assets-builder-sass": "./tasks/sass",
    "assets-builder-svgsymbols": "./tasks/svgsymbols",
    "gulp": "^4.0",
    "gulp-task-maker": "^2.0"
  },
  "browserslist": [
    "defaults",
    "ios >= 9",
    "android >= 5",
    "not ie < 11"
  ]
}
