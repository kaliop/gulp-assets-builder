{
  "name": "assets-builder",
  "private": true,
  "version": "4.1.0",
  "engines": {
    "node": ">=6.0"
  },
  "scripts": {
    "test": "rm gulp-tasks -rf && cp -R ../gulp-tasks . && gulp build",
    "watch": "gulp build && GTM_NOTIFY=1 gulp watch"
  },
  "devDependencies": {
    "gulp": "^3.9",
    "gulp-task-maker": "^1.4"
  },
  "dependencies": {
    "jquery": "^3.2",
    "normalize.css": "^8.0",
    "sass-mq-build": "^1.0"
  }
}
